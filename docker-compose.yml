version: '2'
services:
  postgres:
    image: postgres:9.5.4
    ports:
      - "5432:5432"

  mysql:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
